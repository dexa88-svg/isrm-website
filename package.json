{
  "name": "isrm-website",
  "version": "1.0.0",
  "description": "Interactive Scooter Repair Manuals",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "generate-stats": "node scripts/generate-stats.js"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "<rootDir>/tests/**/*.test.js"
    ],
    "coverageDirectory": "coverage",
    "collectCoverageFrom": [
      "scripts/**/*.js",
      "public/**/*.js",
      "!public/data/**"
    ]
  }
}
