{
  "name": "isrm-website",
  "version": "1.0.0",
  "description": "Interactive Scooter Manuals for Repair",
  "scripts": {
    "dev": "astro dev",
    "build:astro": "astro build",
    "preview": "astro preview",
    "test": "node scripts/validate-content.js && jest",
    "test:coverage": "node scripts/validate-content.js && jest --coverage",
    "validate": "node scripts/validate-content.js",
    "generate-stats": "node scripts/generate-stats.js",
    "search-index": "pagefind --site public --output-path public/pagefind",
    "postinstall": "node --input-type=module --eval \"import './scripts/patch-prerender-dir.mjs'\"",
    "build": "node scripts/validate-content.js && node scripts/generate-stats.js && node --input-type=module --eval \"import './scripts/patch-prerender-dir.mjs'\" && astro build && pagefind --site dist --output-path dist/pagefind"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "pagefind": "^1.5.2"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "<rootDir>/tests/**/*.test.js"
    ],
    "coverageDirectory": "coverage",
    "collectCoverageFrom": [
      "scripts/**/*.js",
      "public/**/*.js",
      "!public/data/**"
    ]
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.3",
    "astro": "^7.1.3",
    "node-html-parser": "^7.1.0",
    "turndown": "^7.2.4"
  }
}
